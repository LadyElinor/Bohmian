# C-WDW-001 Falsification / Re-open Protocol

Date: 2026-03-03
Status: active governance rule

## Current claim under maintenance
- `C-WDW-001` is currently **PROVEN (core envelope)** only.

## Re-open triggers (any one is sufficient)
1. Edge diagnostic reveals correction-term pathology with a mathematically supported mechanism that predicts instability intrusion into current in-core envelope under accepted dt/method policy.
2. Fresh in-core confirmation fails any core gate pair previously required for promotion (proxy/refine/robust-q1/robust-q2/replication/envelope).
3. Independent replication path disagreement exceeds predeclared tolerance in-core after deterministic rerun checks.
4. Null-test failures occur in-core on repeated runs without reproducible recovery.

## Mandatory sequence for cliff investigations
1. Publish derivation appendix section relevant to instability mechanism.
2. Predeclare mechanism-based prediction (what should fail, where, why).
3. Run diagnostic pack.
4. Apply this protocol to decide retain vs reopen.

## Re-open action
If triggered: set `C-WDW-001` status to **OPEN**, add dated rationale in `CLAIM_STATUS_MATRIX.md`, and log blocker + recovery path in monthly gate report.

## Non-trigger note
Edge-only failures without mechanism linking to current in-core envelope do not automatically reopen in-core PROVEN status; they keep edge lane blocked.
